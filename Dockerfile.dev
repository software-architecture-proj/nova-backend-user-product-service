# Dockerfile.dev

FROM golang:1.24

# Install Air for hot reload
RUN go install github.com/air-verse/air@latest

WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

COPY . .

# Air config lives in the container (copied from host via volume)
EXPOSE 50052

CMD ["air"]
